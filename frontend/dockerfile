# Use the official nginx image
FROM nginx:alpine

# Remove the default config
RUN rm /etc/nginx/conf.d/default.conf

# Add a new config that listens on port 8080
COPY nginx.conf /etc/nginx/nginx.conf

# Expose port 8080
EXPOSE 8080
